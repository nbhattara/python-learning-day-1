
here is the sourcecode for finding no of strings from user input text 

def count_vowels(text):
    vowels = "aeiouAEIOU"
    count = sum(1 for char in text if char in vowels)
    return count

input=input("enter  string : ")
print(f"count vowels,{count_vowels(input)}")
